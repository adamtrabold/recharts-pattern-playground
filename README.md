Every recharts setting surfaced into a control on a playground, with quick export functionality for application in a live environment.

Additionally has an interactive pattern generator to experiment pushing data clarity even further.

Built for use at my job (WP Engine), so the color selector defaults to our core design system, Unicorn's, palette.
